{
	"scripts": {
		"serve": "vue-cli-service build && serve -s dist",
		"dev": "concurrently \"pnpm --prefix Frontend run dev\" \"pnpm --prefix Backend run dev\""
	},
	"devDependencies": {
		"concurrently": "^9.1.0"
	}
}
